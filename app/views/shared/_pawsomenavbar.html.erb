<nav class="navbar-transparent fixed-top">
  <div id="logo-set">
    <div id="logo">
      <%= link_to 'Pawsome', root_path, class: "logo_text" %>
      <p>Simple and Ethical Adoption</p>
      <%#= cl_image_tag("PAW_jaq0mh.png", height: 150, width: 150, crop: :fit) %>
    </div>
  </div>

      <!-- <div class="form-inline">
        </%= form_tag pets_path, method: :get do %>
          </%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a pet" %>
            </%= button_tag type: 'submit' , class: "btn btn-outline-light" do %>
              <i class="fas fa-toilet-paper"></i>
            </% end %>
        </% end %>
      </div> -->

  <div id="sign-up-section" style="display:flex">
    <% if current_user %>
      <%= link_to 'Profile', profile_path, class: "sign-up" %>
      <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "sign-up" %>
      <p id="logged">
        <%= current_user.unread_messages %>
        Hello ,  <%= current_user.first_name %>
      </p>

          <!-- <a class="dropdown-item" href="#">Action</a> -->
        </div>
      </div>
      <% if current_user.photo.attached? %>
        <%= cl_image_tag current_user.photo.key, crop: :fill, class: "avatar-airpnp" %>
      <% else %>
      <% end %>
    <% else %>
      <%= link_to 'Sign up', new_user_registration_path, class: "sign-up" %>
      <%= link_to 'Login', new_user_session_path, class: "sign-up" %>
    <% end %>
  </div>
</nav>
